# Always install devDependencies, even when NODE_ENV=production — the build
# step (nest build) needs @nestjs/cli + typescript. Runtime prod behavior is
# controlled by NODE_ENV, not by which packages are installed.
include=dev
